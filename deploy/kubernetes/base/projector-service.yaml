apiVersion: v1
kind: Service
metadata:
  name: projector
  namespace: voting-platform
spec:
  selector:
    app: projector
  ports:
    - name: http
      port: 80
      targetPort: 8081
  type: ClusterIP
